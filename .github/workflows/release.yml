# Release pipeline for the `abstracttui` crate family.
#
# Triggers:
#   - push of a v* tag (e.g. v0.2.13): verify -> publish core to
#     crates.io -> publish the extension family -> GitHub Release.
#   - workflow_dispatch: verify only by default (a release rehearsal);
#     set the `publish` input to true to actually publish from the ref
#     you run it on.
#
# FAMILY SHAPE (ADR-0004 §3: publish order core-first, family the same
# day). The family publishes in the SAME v* tag flow, as a separate
# job AFTER core, chosen over a second tag scheme so one tag stays one
# release event and the core path cannot regress:
#   - `publish` (core) is byte-for-byte the pre-family job — it neither
#     knows nor waits for the family.
#   - `publish-family` runs after it and publishes abstracttui-graph,
#     then abstracttui-mermaid (mermaid DEPENDS on graph, so the order
#     is load-bearing; `cargo publish` blocks until the just-published
#     crate is visible in the index, and a belt retry covers slow
#     index propagation).
#   - Every family publish is SKIPPED when that exact version is
#     already on the index, so a core-only release (family versions
#     unchanged) re-runs green instead of failing on "already
#     published". Bump a family crate's version to release it.
#   - BOTH publish jobs end by ASSERTING their versions are served by
#     the sparse index — job success MEANS published. This is the guard
#     against phantom releases (v0.3.2 went green without publishing:
#     its skip probe read the local workspace, not the registry).
#   - `github-release` depends only on core publish: a family failure
#     is visible (red job) but never blocks the core release record.
#
# AUTH: both publishing jobs exchange GitHub's OIDC identity for a
# short-lived crates.io token through Trusted Publishing. crates.io has
# a matching publisher entry for each crate in the family, keyed to
# lpalbou/AbstractTUI, release.yml, and the crates-io environment. The
# family job's exchange authorizes both extension crates. No long-lived
# crates.io token is stored in GitHub or supplied by a local machine.

name: Release

on:
  push:
    tags: ["v*"]
  workflow_dispatch:
    inputs:
      publish:
        description: "Actually publish to crates.io (otherwise verify only)"
        type: boolean
        default: false

permissions:
  contents: read

env:
  RUST_BACKTRACE: "1"

jobs:
  verify:
    name: verify (tests, lints, package)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      - uses: Swatinem/rust-cache@v2
      - name: Build (all targets, whole workspace)
        run: cargo build --workspace --all-targets
      # A red family blocks the tag on purpose — the same-day family
      # release rides it (ADR-0004).
      - name: Test (whole workspace)
        run: cargo test --workspace
      - name: Doc tests
        run: cargo test --workspace --doc
      - name: Clippy (deny warnings)
        run: cargo clippy --workspace --all-targets -- -D warnings
      - name: Rustdoc (deny warnings)
        run: cargo doc --workspace --no-deps
        env:
          RUSTDOCFLAGS: -D warnings
      - name: Assert tag matches core Cargo.toml version
        # Only meaningful on tag pushes; a workflow_dispatch run has no
        # tag. Select core BY NAME: under the workspace, package order
        # in `cargo metadata` is not core-first.
        if: github.ref_type == 'tag'
        run: |
          crate_version="$(cargo metadata --no-deps --format-version 1 \
            | jq -r '.packages[] | select(.name == "abstracttui") | .version')"
          tag_version="${GITHUB_REF_NAME#v}"
          if [ "$tag_version" != "$crate_version" ]; then
            echo "::error::Tag ${GITHUB_REF_NAME} does not match core version ${crate_version}"
            exit 1
          fi
          echo "Tag ${GITHUB_REF_NAME} matches core version ${crate_version}"
      - name: Package contents sanity check (core + graph)
        # mermaid is deliberately absent here: its packaged form
        # resolves abstracttui-graph from the index, which only works
        # once graph is published — the family job handles it in order.
        run: |
          cargo package -p abstracttui --no-verify --list
          cargo package -p abstracttui-graph --no-verify --list
      - name: Publish dry run (core)
        run: cargo publish -p abstracttui --dry-run

  publish:
    name: publish core to crates.io
    needs: verify
    # Publish on tag pushes, or on manual dispatch when publish=true.
    if: github.ref_type == 'tag' || (github.event_name == 'workflow_dispatch' && inputs.publish)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    # The environment is part of the publishing trust config and lets
    # you attach required-reviewer protection rules.
    environment: crates-io
    permissions:
      contents: read
      id-token: write # crates.io Trusted Publishing OIDC exchange
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
      - name: Authenticate with crates.io (Trusted Publishing)
        id: auth
        uses: rust-lang/crates-io-auth-action@v1
      - name: cargo publish (core, tolerate an already-published version)
        # Idempotent so a version already on crates.io re-runs GREEN
        # instead of failing the tag flow: the maintainer sometimes
        # publishes core locally first (0.3.1 was), and the tag push must
        # still mint the GitHub Release record that `github-release`
        # depends on.
        #
        # ALWAYS ATTEMPT THE PUBLISH. The previous shape asked
        # `cargo info "abstracttui@${version}"` first and skipped when it
        # exited zero — and inside a checkout of this repo it ALWAYS
        # exits zero, because `cargo info` resolves the LOCAL WORKSPACE
        # member and the version being released is, by definition, the
        # workspace version. The guard therefore skipped every genuine
        # release (v0.3.2 went green and published nothing; v0.3.1 only
        # landed because it was published locally first). Two rules fall
        # out, both load-bearing:
        #   1. a decide-then-skip guard must never be the only thing
        #      between a tag and the registry — attempt, then excuse a
        #      failure against the index cargo itself reads;
        #   2. the job may only SUCCEED once the version is verifiably
        #      on that index (the assertion step below) — so any future
        #      guard bug fails loudly instead of minting a phantom
        #      release.
        run: |
          set -euo pipefail
          version=$(cargo pkgid -p abstracttui | sed 's/.*[#@]//')
          echo "publishing abstracttui ${version}"
          if cargo publish -p abstracttui; then
            echo "published abstracttui ${version}"
          else
            status=$?
            if curl -sS --fail https://index.crates.io/ab/st/abstracttui \
              | grep -q "\"vers\":\"${version}\""; then
              echo "abstracttui ${version} is already on the index — treating as published"
            else
              echo "publish failed (exit ${status}) and ${version} is NOT on the index"
              exit "${status}"
            fi
          fi
        env:
          # Ephemeral token minted for this job and revoked by the
          # authentication action when the job ends.
          CARGO_REGISTRY_TOKEN: ${{ steps.auth.outputs.token }}
      - name: assert the version is on the index (job success means published)
        # The postcondition that makes a phantom release impossible: no
        # code path above — publish, excuse, or any future guard — can
        # let this job go green unless the exact version is served by the
        # sparse index cargo resolves from. `cargo publish` blocks until
        # index visibility, so the first probe normally passes; the retry
        # covers propagation tail-latency only.
        run: |
          set -euo pipefail
          version=$(cargo pkgid -p abstracttui | sed 's/.*[#@]//')
          for i in $(seq 1 12); do
            if curl -sS --fail https://index.crates.io/ab/st/abstracttui \
              | grep -q "\"vers\":\"${version}\""; then
              echo "verified: abstracttui ${version} is on the index"
              exit 0
            fi
            echo "not on the index yet (attempt ${i}/12) — waiting 10s"
            sleep 10
          done
          echo "abstracttui ${version} never appeared on the index — the release did NOT happen"
          exit 1

  publish-family:
    name: publish extension family
    needs: publish
    if: github.ref_type == 'tag' || (github.event_name == 'workflow_dispatch' && inputs.publish)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    environment: crates-io
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
      - name: Authenticate with crates.io (Trusted Publishing)
        id: auth
        uses: rust-lang/crates-io-auth-action@v1
      - name: Publish family crates in dependency order (skip already-published versions)
        run: |
          publish_if_absent() {
            crate="$1"
            version="$(cargo metadata --no-deps --format-version 1 \
              | jq -r ".packages[] | select(.name == \"$crate\") | .version")"
            # Sparse-index probe: names >= 4 chars live at <a><b>/<c><d>/<name>.
            prefix="$(printf '%s' "$crate" | cut -c1-2)/$(printf '%s' "$crate" | cut -c3-4)"
            index="$(curl -sSL "https://index.crates.io/${prefix}/${crate}" || true)"
            if printf '%s' "$index" | grep -q "\"vers\":\"${version}\""; then
              echo "${crate} ${version} already on crates.io — skipping (bump the version to release)"
              return 0
            fi
            echo "Publishing ${crate} ${version}"
            # cargo publish blocks until the new version is visible in
            # the index; the retry is a belt for slow propagation of a
            # JUST-published dependency (graph -> mermaid).
            cargo publish -p "$crate" || { sleep 45; cargo publish -p "$crate"; }
          }
          publish_if_absent abstracttui-graph
          publish_if_absent abstracttui-mermaid
        env:
          # One exchange covers every family crate whose crates.io
          # publisher entry matches this workflow identity.
          CARGO_REGISTRY_TOKEN: ${{ steps.auth.outputs.token }}
      - name: assert family versions are on the index
        # Same postcondition as core: whether a crate was published just
        # now or skipped as already-present, the workspace version must
        # be served by the index for this job to succeed.
        run: |
          set -euo pipefail
          for crate in abstracttui-graph abstracttui-mermaid; do
            version="$(cargo metadata --no-deps --format-version 1 \
              | jq -r ".packages[] | select(.name == \"$crate\") | .version")"
            prefix="$(printf '%s' "$crate" | cut -c1-2)/$(printf '%s' "$crate" | cut -c3-4)"
            ok=0
            for i in $(seq 1 12); do
              if curl -sS --fail "https://index.crates.io/${prefix}/${crate}" \
                | grep -q "\"vers\":\"${version}\""; then
                echo "verified: ${crate} ${version} is on the index"
                ok=1
                break
              fi
              echo "${crate} ${version} not on the index yet (attempt ${i}/12) — waiting 10s"
              sleep 10
            done
            if [ "$ok" != "1" ]; then
              echo "${crate} ${version} never appeared on the index"
              exit 1
            fi
          done

  github-release:
    name: create GitHub Release
    needs: publish
    # Only tags have something to attach release notes to.
    if: github.ref_type == 'tag'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: write
    steps:
      - name: Create release with auto-generated notes
        uses: softprops/action-gh-release@v3
        with:
          generate_release_notes: true
